export const fetchDepartamentPending = (state, action) => {};

export const fetchDepartamentFulfilled = (state, action) => {};

export const fetchDepartamentRejected = (state, action) => {};

export const cleanDepartamentStory = (state, action) => {};
